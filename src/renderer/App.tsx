import type { ReactElement } from "react";

// Placeholder root component for the Setup phase build pipeline (T001-T013).
// Replaced by the real editor shell in T083.
export function App(): ReactElement {
  return <div>Solari AI Sanitizer</div>;
}
